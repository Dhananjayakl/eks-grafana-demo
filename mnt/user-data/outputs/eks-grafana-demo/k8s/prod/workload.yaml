---
apiVersion: v1
kind: Namespace
metadata:
  name: prod
  labels:
    environment: prod
    monitoring: "true"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: sample-app
  namespace: prod
  labels:
    app: sample-app
    environment: prod
spec:
  replicas: 3
  selector:
    matchLabels:
      app: sample-app
  template:
    metadata:
      labels:
        app: sample-app
        environment: prod
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "80"
    spec:
      containers:
        - name: nginx
          image: nginx:1.25-alpine
          ports:
            - containerPort: 80
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "200m"
              memory: "256Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: sample-app
  namespace: prod
  labels:
    app: sample-app
    environment: prod
spec:
  selector:
    app: sample-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP
